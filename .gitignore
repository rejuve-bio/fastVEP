# External tools
ensembl-vep/
Nirvana/
echtvar-src/
echtvar/

# Build
target/
.DS_Store

# Conda build artifacts
conda/recipe/build/
conda-bld/

# Large data (downloaded via benchmarks/download_data.sh)
test_data/

# Root test directory contains large downloaded data files — tracked files are force-added via git add -f
/tests/

# Benchmark data and output (scripts are tracked)
benchmarks/output/
benchmarks/*.fastvep.cache

# Auto-generated caches
*.fastvep.cache

# data
genomes/*
.gitignore

# docs
*stats.json
*docx
# Local benchmark data (downloaded sources, built .osa, intermediate VCFs)
data/

# tabix index files cached during remote queries (placed in CWD by htslib)
*.tbi
*.csi
.venv
.env
api/.env
__pycache__/
.pytest_cache/
*.pyc
